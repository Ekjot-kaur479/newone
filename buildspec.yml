version: 0.2

phases:
  install:
    commands:
      - echo "Nothing to install for HTML site."

  build:
    commands:
      - echo "Nothing to build for HTML site."

artifacts:
  files:
    - index.html

